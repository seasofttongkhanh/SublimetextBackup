<snippet>
    <content><![CDATA[
function parseDate(date, space = '-'){
    date = new Date(date);
    var day = date.getDate();
    var day = (String(day).length != 1)? day : '0'+day;
    var month = date.getMonth() + 1;
    var month = (String(month).length != 1)? month : '0'+month;
    var year = date.getFullYear();
    return day+space+month+space+year;
}]]></content>
    <tabTrigger>js-parse-date</tabTrigger>
    <description>js</description>
</snippet>